git clone https://github.com/sjain-stanford/CS231N

git config --global user.email "sambhav.eee@gmail.com"
git config --global user.name "Sambhav Jain"


jupyter notebook ‐‐generate‐config

# Edit ~/.jupyter/jupyter_notebook_config.py to add this:
c = get_config()
c.NotebookApp.ip = '*'
c.NotebookApp.open_browser = False
c.NotebookApp.port = 7000


# To push local changes to git repo
git add *
git commit -m "Comment"
git push origin master

# To fetch changes and overwrite if conflict
git add *
git commit -m "Comment"
git fetch origin master
git merge -s recursive -X theirs origin/master    # use 'their' changes if conflict
git merge -s recursive -X your origin/master      # use 'your' changes if conflict

